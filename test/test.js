const assert = require('power-assert');

describe('ES6 test of test', () => {
  it('Sample: Add calculation', () => {
    const expect = 2;
    const actual = 1 + 1;

    assert(expect === actual);
  });
});
